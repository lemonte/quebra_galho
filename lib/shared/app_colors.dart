import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  static const Color primaryColorApp = Color(0xFFFF9800);
  static const Color iconsColorApp = Colors.grey;
  static const Color textColor = Color.fromARGB(255, 75, 75, 75);
}
